# Harmony v1.3.0 开发计划

## 阶段一：核心兼容能力

- [x] 付费模型保持可见，但未授权时禁止选中、切换和作为新任务默认模型。
- [x] 工具结果压缩标记转成可读提示，不当作工具失败；模型停止标记和内部引用标记
  在完整消息与流式展示边界清理。
- [x] 保存并恢复远端运行时模型、供应商、推理强度、快速模式及待生效状态。
- [x] Markdown 普通文本中的裸 HTTP(S) URL 自动转为链接，保留尾部标点和不匹配括号。

## 阶段二：设置页对齐

- [x] 账号头部、Relay 状态、版本、通知、语音词典、语言、关于本机、调试和法律分组。
- [x] 设备名称编辑、语音词典只读同步、账号注销验证流程和法律链接。
- [x] Light / Dark 统一使用 Harmony 语义颜色 token，并采用 Mobile 的卡片、分组标题、
  行高和 inset 分隔线节奏。
- [x] Huawei Push Token 保持既有业务逻辑，仅调整分组位置与视觉呈现。

## 阶段三：验证

- [x] `devecocli build` 构建通过。
- [x] `entry` Local Test 通过。
- [ ] 窄屏、展开横屏、Light、Dark、字体放大和减弱动态效果完成设备目检。
- [ ] 使用现有签名配置生成并覆盖安装 `entry-default-signed.hap`。
