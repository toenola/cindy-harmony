{
  "name": "@cindy/device-link-protocol",
  "version": "0.0.0",
  "private": true,
  "description": "device-link relay-layer wire protocol (single source of truth): envelope, routing semantics, connection-layer payloads. Zero runtime deps",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./protocol": "./src/protocol.ts"
  },
  "scripts": {
    "build": "tsc --noEmit"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
