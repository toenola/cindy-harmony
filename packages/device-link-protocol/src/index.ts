/**
 * @cindy/device-link-protocol
 * ---------------------------------------------------------------------------
 * device-link 中继层(relay)协议的单一权威来源:信封、路由语义、连接层 payload。
 * relay server 与客户端 device-link 包共同引用;详见 ./protocol.ts 头注。
 */

export * from './protocol';
