{
  "name": "@cindy/maker-pi-manager",
  "version": "0.0.0",
  "private": true,
  "description": "Cindy pi-remote: NDJSON RPC daemon spawned on remote SSH machines. Holds pi --mode rpc child processes (one per session), bridges stdio over unix sockets. Multi-session singleton + detach/reattach + conditional restart + idle recycling. Zero Electron deps.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/client.ts",
    "./protocol": "./src/protocol.ts",
    "./codec": "./src/codec.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "bundle": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "author": {
    "name": "XD Inc.",
    "email": "feedback@cindy.app"
  },
  "license": "Apache-2.0",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "*",
    "esbuild": "^0.28.1",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
